apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm.fullname" . }}-auth
  labels:
  {{- include "helm.labels" . | nindent 4 }}
stringData:
  config.yaml: {{ required "auth.configYaml is required" .Values.auth.configYaml
    | quote }}
type: Opaque
