apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: sreportal
    app.kubernetes.io/managed-by: kustomize
  name: web-mcp-service
  namespace: system
spec:
  ports:
    - name: web
      port: 8090
      protocol: TCP
      targetPort: 8090
    - name: mcp
      port: 8091
      protocol: TCP
      targetPort: 8091
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: sreportal
