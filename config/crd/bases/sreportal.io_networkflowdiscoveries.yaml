---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: networkflowdiscoveries.sreportal.io
spec:
  group: sreportal.io
  names:
    kind: NetworkFlowDiscovery
    listKind: NetworkFlowDiscoveryList
    plural: networkflowdiscoveries
    singular: networkflowdiscovery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.portalRef
      name: Portal
      type: string
    - jsonPath: .status.nodes
      name: Nodes
      type: integer
    - jsonPath: .status.edges
      name: Edges
      type: integer
    - jsonPath: .status.lastReconcileTime
      name: Last Reconcile
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkFlowDiscovery is the Schema for the networkflowdiscoveries API.
          It discovers network flows from Kubernetes NetworkPolicies and FQDNNetworkPolicies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NetworkFlowDiscovery
            properties:
              namespaces:
                description: |-
                  namespaces is an optional list of namespaces to scan.
                  When empty, all namespaces are scanned.
                items:
                  type: string
                type: array
              portalRef:
                description: portalRef is the name of the Portal this resource is
                  linked to
                minLength: 1
                type: string
            required:
            - portalRef
            type: object
          status:
            description: status defines the observed state of NetworkFlowDiscovery
            properties:
              conditions:
                description: conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              edges:
                description: edges are the directional flow relations between nodes
                items:
                  description: FlowEdge represents a directional flow between two
                    nodes.
                  properties:
                    edgeType:
                      description: edgeType describes the flow type (e.g. internal,
                        cross-ns, cron, database, messaging, external)
                      type: string
                    from:
                      description: from is the source node id
                      type: string
                    to:
                      description: to is the target node id
                      type: string
                  required:
                  - edgeType
                  - from
                  - to
                  type: object
                type: array
              lastReconcileTime:
                description: lastReconcileTime is the timestamp of the last reconciliation
                format: date-time
                type: string
              nodes:
                description: nodes are all discovered services, databases, crons,
                  and external endpoints
                items:
                  description: FlowNode represents a service, database, cron job,
                    or external endpoint.
                  properties:
                    group:
                      description: group is the logical group (namespace name by default)
                      type: string
                    id:
                      description: id is the unique node identifier (e.g. "service:core:my-account-api")
                      type: string
                    label:
                      description: label is the human-readable name
                      type: string
                    namespace:
                      description: namespace is the Kubernetes namespace
                      type: string
                    nodeType:
                      description: 'nodeType is one of: service, cron, database, messaging,
                        external'
                      type: string
                  required:
                  - group
                  - id
                  - label
                  - namespace
                  - nodeType
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
