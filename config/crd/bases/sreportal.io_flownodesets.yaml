---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: flownodesets.sreportal.io
spec:
  group: sreportal.io
  names:
    kind: FlowNodeSet
    listKind: FlowNodeSetList
    plural: flownodesets
    singular: flownodeset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.discoveryRef
      name: Discovery
      type: string
    - jsonPath: .status.nodes
      name: Nodes
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FlowNodeSet stores the discovered flow nodes for a NetworkFlowDiscovery
          resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FlowNodeSetSpec defines the desired state of FlowNodeSet.
            properties:
              discoveryRef:
                description: discoveryRef is the name of the parent NetworkFlowDiscovery
                  resource
                minLength: 1
                type: string
            required:
            - discoveryRef
            type: object
          status:
            description: FlowNodeSetStatus defines the observed state of FlowNodeSet.
            properties:
              nodes:
                description: nodes are all discovered services, databases, crons,
                  and external endpoints
                items:
                  description: FlowNode represents a service, database, cron job,
                    or external endpoint.
                  properties:
                    group:
                      description: group is the logical group (namespace name by default)
                      type: string
                    id:
                      description: id is the unique node identifier (e.g. "service:core:my-account-api")
                      type: string
                    label:
                      description: label is the human-readable name
                      type: string
                    namespace:
                      description: namespace is the Kubernetes namespace
                      type: string
                    nodeType:
                      description: 'nodeType is one of: service, cron, database, messaging,
                        external'
                      type: string
                  required:
                  - group
                  - id
                  - label
                  - namespace
                  - nodeType
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
