---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: dns.sreportal.my.domain
spec:
  group: sreportal.my.domain
  names:
    kind: DNS
    listKind: DNSList
    plural: dns
    singular: dns
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DNS is the Schema for the dns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DNS
            properties:
              groups:
                description: groups is a list of DNS entry groups for organizing entries
                  in the UI
                items:
                  description: DNSGroup represents a group of DNS entries
                  properties:
                    description:
                      description: description is an optional description for the
                        group
                      type: string
                    entries:
                      description: entries is a list of DNS entries in this group
                      items:
                        description: DNSEntry represents a manual DNS entry
                        properties:
                          description:
                            description: description is an optional description for
                              the DNS entry
                            type: string
                          fqdn:
                            description: fqdn is the fully qualified domain name
                            minLength: 1
                            type: string
                        required:
                        - fqdn
                        type: object
                      type: array
                    name:
                      description: name is the display name for this group
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              portalRef:
                description: portalRef is the name of the Portal this DNS resource
                  is linked to
                minLength: 1
                type: string
            required:
            - portalRef
            type: object
          status:
            description: status defines the observed state of DNS
            properties:
              conditions:
                description: conditions represent the current state of the DNS resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              groups:
                description: groups is the list of FQDN groups with their status
                items:
                  description: FQDNGroupStatus represents a group of FQDNs in the
                    status
                  properties:
                    description:
                      description: description is the group description
                      type: string
                    fqdns:
                      description: fqdns is the list of FQDNs in this group
                      items:
                        description: FQDNStatus represents the status of an aggregated
                          FQDN
                        properties:
                          description:
                            description: description is an optional description for
                              the FQDN
                            type: string
                          fqdn:
                            description: fqdn is the fully qualified domain name
                            type: string
                          lastSeen:
                            description: lastSeen is the timestamp when this FQDN
                              was last observed
                            format: date-time
                            type: string
                          recordType:
                            description: recordType is the DNS record type (A, AAAA,
                              CNAME, etc.)
                            type: string
                          targets:
                            description: targets is the list of target addresses for
                              this FQDN
                            items:
                              type: string
                            type: array
                        required:
                        - fqdn
                        - lastSeen
                        type: object
                      type: array
                    name:
                      description: name is the group name
                      type: string
                    source:
                      description: source indicates where this group came from (manual
                        or external-dns)
                      enum:
                      - manual
                      - external-dns
                      type: string
                  required:
                  - name
                  - source
                  type: object
                type: array
              lastReconcileTime:
                description: lastReconcileTime is the timestamp of the last reconciliation
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
