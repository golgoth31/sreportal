# Example auth Secret for write endpoint authentication.
# Enable by uncommenting the auth section in config/default/kustomization.yaml.
# Configure at least one method (basicAuth, apiKey, jwt).
apiVersion: v1
kind: Secret
metadata:
  name: auth
  namespace: system
type: Opaque
stringData:
  config.yaml: |
    # basicAuth:
    #   enabled: true
    #   username: admin
    #   password: changeme
    # apiKey:
    #   enabled: true
    #   keys:
    #     - name: ci-pipeline
    #       key: "your-api-key-here"
    # jwt:
    #   enabled: true
    #   issuers:
    #     - name: auth0
    #       issuerURL: https://example.auth0.com/
    #       audience: sreportal-api
    #       jwksURL: https://example.auth0.com/.well-known/jwks.json
    #       requiredClaims:
    #         scope: "release:write"
